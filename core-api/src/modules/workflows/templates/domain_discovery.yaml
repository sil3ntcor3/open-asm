name: vulnerability_scan_basic

on:
  target: [domain.create, domain.re-scan]

jobs:
  - name: Scan Subdomain
    run: subfinder

  - name: Port Scan
    run: naabu

  - name: Service Discovery
    run: nmap

  - name: HTTP Probe
    run: httpx

  - name: Take Screenshot
    run: screenshot

  - name: Vuls Scan
    run: nuclei